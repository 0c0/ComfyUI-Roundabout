# 打包/发布（comfy node publish 等）时额外排除的文件（语法同 .gitignore）。
# 本仓库当前不走 Registry 发布，保留此文件是为了万一发布时不会带上开发脚本。
#
# 打包默认以「git 跟踪的文件」为准，所以 .env / .cache / test_outputs / *.7z /
# *.bak 这些未跟踪文件本来就不会进包；这里只排除**已跟踪但不该随节点分发**的
# 开发自测脚本（里面写死了本机路径与端口，对使用者没有价值）。

tests/
