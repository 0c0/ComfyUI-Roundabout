# ComfyUI-Roundabout 依赖
#
# ComfyUI 自身已带 aiohttp / pydantic / pyyaml，这里只声明它不带的部分。
# MCP 网关默认启用，因此这两个包默认就需要装；若设 MCP_ENABLED=false
# 只用 REST 网关，则可以不装。
#
# 注意：mcp 2.x 提供 mcp.server.MCPServer 与 streamable_http_app，
# 与 1.x（FastMCP）API 不兼容，必须 >=2.0.0。
# mcp 2.0.0 自身已依赖 uvicorn，这里显式声明是因为 mcp_server.py 直接 import uvicorn。
mcp>=2.0.0
uvicorn>=0.30.0
